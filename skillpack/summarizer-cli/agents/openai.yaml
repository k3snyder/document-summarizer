interface:
  display_name: "Summarizer CLI"
  short_description: "Run docs through Document Summarizer headlessly"
  default_prompt: "Use $summarizer-cli to process documents with the headless CLI when available, falling back to the desktop app only when History visibility is required. Honor the favorites gate before processing; automation wrappers should pass --require-favorite with an explicit --favorite."
